This folder is needed because we have to point the web server to
where our js files are installed.

We can get ridd of this folder when our web server will have full search
path capability and we don't need this anymore.
